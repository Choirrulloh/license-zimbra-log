<!DOCTYPE html>
<html>
<head>
  <title><%= viewUser.name %> - User Details</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100">
  <div class="flex h-screen overflow-hidden">
    <aside class="w-64 bg-gray-800 text-white flex-shrink-0">
      <div class="p-4">
        <h1 class="text-2xl font-bold mb-8"><i class="fas fa-key"></i> License Manager</h1>
        <nav class="space-y-2">
          <a href="/dashboard" class="flex items-center space-x-3 p-3 rounded hover:bg-gray-700"><i class="fas fa-chart-line w-5"></i><span>Dashboard</span></a>
          <a href="/products" class="flex items-center space-x-3 p-3 rounded hover:bg-gray-700"><i class="fas fa-box w-5"></i><span>Products</span></a>
          <a href="/customers" class="flex items-center space-x-3 p-3 rounded hover:bg-gray-700"><i class="fas fa-users w-5"></i><span>Customers</span></a>
          <a href="/licenses" class="flex items-center space-x-3 p-3 rounded hover:bg-gray-700"><i class="fas fa-certificate w-5"></i><span>Licenses</span></a>
          <a href="/users" class="flex items-center space-x-3 p-3 rounded bg-gray-700"><i class="fas fa-user-shield w-5"></i><span>Users</span></a>
          <a href="/reports" class="flex items-center space-x-3 p-3 rounded hover:bg-gray-700"><i class="fas fa-file-alt w-5"></i><span>Reports</span></a>
          <a href="/docs" class="flex items-center space-x-3 p-3 rounded hover:bg-gray-700"><i class="fas fa-book w-5"></i><span>API Docs</span></a>
        </nav>
      </div>
      <div class="absolute bottom-0 w-64 p-4 bg-gray-900">
        <div class="flex items-center space-x-3">
          <div class="w-10 h-10 rounded-full bg-gray-700 flex items-center justify-center"><i class="fas fa-user"></i></div>
          <div class="flex-1 min-w-0">
            <p class="text-sm font-medium truncate"><%= user.userName || 'Admin' %></p>
            <p class="text-xs text-gray-400 truncate"><%= user.userEmail || '' %></p>
          </div>
          <a href="/logout" class="text-gray-400 hover:text-white"><i class="fas fa-sign-out-alt"></i></a>
        </div>
      </div>
    </aside>

    <div class="flex-1 flex flex-col overflow-hidden">
      <header class="bg-white shadow-sm">
        <div class="flex items-center justify-between p-4">
          <h2 class="text-xl font-semibold text-gray-800">User Details</h2>
          <div class="space-x-2">
            <a href="/users/<%= viewUser.id %>/edit" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg text-sm">
              <i class="fas fa-edit"></i> Edit
            </a>
            <a href="/users" class="text-gray-600 hover:text-gray-800 text-sm">
              <i class="fas fa-arrow-left"></i> Back
            </a>
          </div>
        </div>
      </header>

      <main class="flex-1 overflow-y-auto p-6">
        <!-- User Info Card -->
        <div class="bg-white rounded-lg shadow p-6 mb-6">
          <div class="flex items-start space-x-4">
            <div class="w-20 h-20 rounded-full bg-gray-200 flex items-center justify-center">
              <i class="fas fa-user text-3xl text-gray-500"></i>
            </div>
            <div class="flex-1">
              <div class="flex items-center justify-between">
                <h3 class="text-2xl font-bold text-gray-800"><%= viewUser.name %></h3>
                <span class="px-3 py-1 rounded-full text-sm font-semibold <%= viewUser.is_active ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
                  <%= viewUser.is_active ? 'Active' : 'Inactive' %>
                </span>
              </div>
              <p class="text-gray-600 mt-1"><%= viewUser.email %></p>
              <div class="mt-4 grid grid-cols-1 md:grid-cols-3 gap-4">
                <div>
                  <p class="text-xs text-gray-500 uppercase">Role</p>
                  <p class="mt-1">
                    <span class="px-2 py-1 rounded-full text-xs font-semibold <%= viewUser.role === 'super_admin' ? 'bg-purple-100 text-purple-800' : viewUser.role === 'admin' ? 'bg-blue-100 text-blue-800' : 'bg-gray-100 text-gray-800' %>">
                      <%= viewUser.role %>
                    </span>
                  </p>
                </div>
                <div>
                  <p class="text-xs text-gray-500 uppercase">Last Login</p>
                  <p class="mt-1 text-sm">
                    <%= viewUser.last_login ? moment(viewUser.last_login).format('MMM D, YYYY HH:mm') : 'Never' %>
                  </p>
                </div>
                <div>
                  <p class="text-xs text-gray-500 uppercase">Phone</p>
                  <p class="mt-1 text-sm"><%= viewUser.phone || '-' %></p>
                </div>
                <div>
                  <p class="text-xs text-gray-500 uppercase">Created</p>
                  <p class="mt-1 text-sm"><%= moment(viewUser.created_at).format('MMM D, YYYY') %></p>
                </div>
                <div>
                  <p class="text-xs text-gray-500 uppercase">Updated</p>
                  <p class="mt-1 text-sm"><%= moment(viewUser.updated_at).format('MMM D, YYYY') %></p>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Activity History -->
        <div class="bg-white rounded-lg shadow">
          <div class="p-6 border-b">
            <h3 class="text-lg font-semibold">Recent Activity</h3>
          </div>
          <div class="p-6">
            <% if (activities.length > 0) { %>
              <div class="space-y-4">
                <% activities.forEach(activity => { %>
                  <div class="flex items-start space-x-3 pb-4 border-b last:border-b-0">
                    <div class="flex-shrink-0">
                      <div class="w-10 h-10 rounded-full <%= activity.action === 'login' ? 'bg-green-100' : activity.action === 'logout' ? 'bg-gray-100' : activity.action === 'create' ? 'bg-blue-100' : activity.action === 'update' ? 'bg-yellow-100' : 'bg-red-100' %> flex items-center justify-center">
                        <i class="fas <%= activity.action === 'login' ? 'fa-sign-in-alt text-green-600' : activity.action === 'logout' ? 'fa-sign-out-alt text-gray-600' : activity.action === 'create' ? 'fa-plus text-blue-600' : activity.action === 'update' ? 'fa-edit text-yellow-600' : 'fa-trash text-red-600' %>"></i>
                      </div>
                    </div>
                    <div class="flex-1 min-w-0">
                      <p class="text-sm font-medium text-gray-900">
                        <%= activity.description %>
                      </p>
                      <% if (activity.entity_type && activity.entity_id) { %>
                        <p class="text-xs text-gray-500 mt-1">
                          <%= activity.entity_type %> #<%= activity.entity_id %>
                        </p>
                      <% } %>
                      <% if (activity.ip_address) { %>
                        <p class="text-xs text-gray-500 mt-1">
                          <i class="fas fa-network-wired"></i> <%= activity.ip_address %>
                        </p>
                      <% } %>
                    </div>
                    <div class="flex-shrink-0 text-right">
                      <p class="text-xs text-gray-500">
                        <%= moment(activity.created_at).format('MMM D, YYYY') %>
                      </p>
                      <p class="text-xs text-gray-400">
                        <%= moment(activity.created_at).format('HH:mm') %>
                      </p>
                    </div>
                  </div>
                <% }); %>
              </div>
            <% } else { %>
              <p class="text-center text-gray-500 py-8">No activity recorded yet</p>
            <% } %>
          </div>
        </div>
      </main>
    </div>
  </div>
</body>
</html>
